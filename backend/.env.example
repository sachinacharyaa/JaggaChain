PORT=5000
MONGO_URI=mongodb+srv://...
# Optional: record actions on Solana (memo transactions). Leave empty for dev placeholders.
SOLANA_RPC_URL=https://api.devnet.solana.org
# Base58-encoded keypair (wallet with SOL for tx fees). Generate with: solana keypair new
SOLANA_MINT_KEYPAIR=
