PORT=5000
MONGO_URI=mongodb+srv://...
# Required for fee transactions and on-chain recording (backend builds/submits tx; frontend only signs).
SOLANA_RPC_URL=https://api.devnet.solana.org
# Base58-encoded keypair (wallet with SOL for tx fees + memo/recording). Generate with: solana keypair new
SOLANA_MINT_KEYPAIR=

# Protocol fees (SOL): Citizen 0.02, Land Revenue Officer 0.05, Chief Land Revenue Officer 0.08
FEE_CITIZEN_SOL=0.02
FEE_LRO_SOL=0.05
FEE_CLRO_SOL=0.08
# Treasury Solana address (base58). Must be set for fee payments to work.
TREASURY_WALLET=

# Demo seed data on backend startup (set true only for local demo/testing)
ENABLE_DEMO_SEED=false
